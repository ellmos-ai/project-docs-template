## Last-checked: 2026-09-12

# ellmos-ai/project-docs-template

Agent-ready project documentation template with START/STATE/TODO/DONE,
workflows, lightweight tooling, and LLM-friendly project memory.

Canonical repository:
https://github.com/ellmos-ai/project-docs-template

Primary audience:
- Maintainers who want predictable LLM-agent handoff files in a repo root.
- Claude Code, Antigravity/Gemini, Codex, and other AI coding-agent users.
- Teams that need lightweight project state, decision, task, and workflow docs.
- Template authors looking for a small alternative to heavy agent OS scaffolds.

Important files:
- `README.md`: human-facing overview, quick navigation, target personas, and comparative architecture.
- `README_de.md`: complete German documentation, Schnellnavigation, and user guide.
- `MARKETING-LOG.txt`: Pfad B discoverability log, target personas, keyword matrix, and positioning audit.
- `THIRD_PARTY_LICENSES.md`: Software Bill of Materials (SBOM) confirming zero runtime dependencies.
- `template/TEMPLATE.md`: rationale, profile definitions, and file-by-file explanation.
- `template/_tools/init-project`: profile-based scaffold generator (`MINIMAL`, `STANDARD`, `FULL`).
- `template/_tools/init-project --upgrade`: one-step, manifest/hash-verified
  profile upgrade for existing generated projects; changed or unowned files
  fail closed rather than being merged.
- `tests/test_tools.py`: cross-profile and tool-integrity regression contract.
- `tests/test_metadata.py`: automated metadata, schema, and manifest parity test suite.
- `RELEASE_GATE.md`: commands and evidence required before release.
- `SECURITY.md`: bilingual vulnerability-reporting policy with Zero-Egress and Local-First guarantees.
- `template/START.md`: session bootstrap pattern.
- `template/STATE.md`: current-state snapshot pattern.
- `template/TODO.md` and `template/DONE.md`: active and completed task surfaces.
- `template/DECISIONS.md` and `template/PATTERNS.md`: decision and practice logs.

## Verification

- `pytest` (34 passed, 7 subtests passed) on Linux, Windows and macOS
- `ruff check .` (100% clean)
- `compileall` (100% clean)

## Search Phrases

```text
agent-ready project documentation template
LLM project docs template START STATE TODO DONE
Claude Code Codex project documentation scaffold
multi-agent repo handoff documentation template
lightweight project memory template for coding agents
project docs template with TODO DONE DECISIONS STATE
zero-egress developer scaffolding stdlib
deterministic documentation staging and profile upgrades
```

## Language

The repository documentation (`README.md`, `llms.txt`, `SECURITY.md`,
`RELEASE_GATE.md`) is English, with a German `README_de.md`. The generated
artefacts are not: the template bodies under `template/` and the CLI output of
the tools in `template/_tools/` are currently German. File names, profile
markers and YAML front matter keys are language-neutral. There is no language
switch; an English template set is an open task in `TODO.md`.

## Disambiguation

This repository is a documentation scaffold for software, research, and
operations projects that are maintained with LLM agents. It is not a runtime
agent framework, not a task runner, and not a replacement for project-specific
source code or CI.

